node_modules/
dist/
.env
.env.local
server/data/transcripts/
server/data/arena/
server/data/scoreboard.json
*.log
.DS_Store
demo/tracer-decisions.db*
# The control run's artifact: regenerated by `tracer demo-proxy --unprotected`,
# and it is a copy of the notes the injection stole. Not something to commit.
demo/workspace/public-share/northwind-audit.txt

# Rendered video clips. Build artifacts: `node scripts/render-clips.mjs` makes
# them from web/tools/clips, which is committed. A repo carrying a hundred
# megabytes of MP4 that one command reproduces is a repo nobody wants to clone.
media/

# Client permissions for the recorded MCP client run (docs/BRIEF-AGENT-CAPTURE.md).
.claude/settings.local.json
